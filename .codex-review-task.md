# HOLP v0.1.3 从头深度 Review（第四轮）— 判断能否进实现

## 背景

第四轮。轨迹:
- v0.1:你判 19 条,「不值得写」。
- v0.1.1:你判「16/19 表面改 + 3 新 P0」,「仍不值得」。
- v0.1.2:你判「yes-with-caveats 偏 no」,列 5 个阻塞缺口。
- **v0.1.3**(本次,commit 952a233):设计者按你 v0.1.2 的 5 个阻塞缺口逐条修——quorum 公式 / artifact 强制 content / approval cancel 语义 / task.cancel schema / capability required scope + 错误码定序 + 跨文件脚本扫净。变更明细见 spec.md 文末「v0.1.2 → v0.1.3 变更明细」。

## 核心任务

两件事:

1. **核实 v0.1.2 的 5 个阻塞缺口在 v0.1.3 是否真修对**(不是表面改)。逐条 ✅真修对 / 🟡半修 / ❌没修。
2. **给最终判断:v0.1.3 能否进实现阶段?**(写协议骨架 + 最小 daemon/consumer + 契约测试)。明确 yes / yes-with-caveats / no。

**公平要求**:前几轮你抓得很准。这轮如果真修对了,给 ✅,别为了「显得严格」报 🟡。但如果还有实现会撞墙的洞,照报。设计者需要的是**可信的「可以写了」或「还差什么」**,不是鼓励也不是挑刺。

## 必读（从头读）

- `protocol/spec.md`(核心)
- `protocol/version.md`
- `adapters/agent-backend.ts`、`adapters/registry.ts`
- `docs/positioning.md`、`README.md`

## Review 重点

### A. 核实 5 个阻塞缺口(v0.1.2 你列的)

1. **quorum 公式**(§6.2):改成「第1段 `panel可用数 ≥ quorum`、不预留作者余量,作者命中 panel 的动态不足交第2段」。这个修法对吗?验证设计者的示例(coder=codex、reviewer panel=[claude,gemini]、quorum=2):第1段 2≥2 通过、第2段作者=codex 不在 panel 所以 eligible=2≥2。逻辑自洽吗?有没有新边界(panel 含 rejected agent 时第1段怎么算、第2段 eligible 计算的精确定义)?
2. **artifact 强制 content**(§8.2):禁 content_ref,永远返回 content(可 truncated)。big artifact 分块留 v0.2。这个兜底可执行吗?truncated_at + envelope.size 够 consumer 判断吗?有没有「content 是 base64 还是 utf-8」的歧义(设计者说 utf-8 文本,二进制 base64)?
3. **approval cancel**(§7):只由 task.cancel 导致,无 approval.cancel 方法。resolved 补 reason,race「先终态者赢」。这套自洽吗?有没有「client 想 abort approval 但不想取消整个 run」的场景没覆盖(这算 v0.2 还是真的漏)?
4. **task.cancel**(§7.5):补了 schema(幂等/run_not_found/终态/race)。够实现吗?对 pending approval 发 approval_cancelled、对 in-flight agent step 经 adapter cancel——这些衔接清楚吗?
5. **capability required scope**(§2):required 一律连接级,run 级走 params,缺省=不支持,kinds 空交集语义。这套规则无歧义吗?client required + server 不支持的 capability,initialize 拒绝的时机和错误码清楚吗?

### B. v0.1.3 有没有新矛盾

- 错误码定序(`invalid_quorum` 形状先于 `quorum_unsatisfiable` 策略)——两段式 quorum + 两个 code,定序在 §6.2 末尾说了,和 §10 表对得上吗?
- §7 approval race「先终态者赢」+ §7.5 task.cancel race「以 server 实际状态为准」——两处 race 描述会不会冲突?
- §8.2 content 强制 + §6.1/§7 的 envelope ref(findings/details)——ref 处带 envelope、内容走 artifact.get 取 content,链路完整闭环吗?

### C. 跨文件一致性(脚本扫过了,你抽查)

设计者这轮用脚本自查了 version.md/README/adapter 注释全对齐 v0.1.x。**抽查确认**:还有没有把「当前态」写成 v0.1.1/v0.1 的残留?version.md 的「Remote 改 opaque object」旧态删了吗?

### D. 照着能实现吗(最关键)

假设你现在动手写:**最小 consumer**(initialize + flock + orchestrate.run + events.subscribe + approval.resolve + task.cancel)+ **最小 daemon**(桩 adapter + 跑协议面 + events 推送)。你会卡在哪?把卡点列出来——区分:
- **「实现前必须修」**(协议级缺口,写代码会撞墙)
- **「实现时可定」**(协议留了口子,实现时拍板即可,不必改 spec)
- **「进 v0.2」**(合理推迟)

## 不需要你做的

- 不要改文件、不要重写 spec。
- 不要因为「第四轮了该放行」就手软,也不要为挑刺而挑刺。

## 输出格式

1. **5 缺口核实表**:逐条 ✅/🟡/❌ + 证据。
2. **新矛盾**(如有):P0/P1/P2。
3. **跨文件抽查**:残留点(带文件:行)或「干净」。
4. **总判断**:
   - v0.1.2 → v0.1.3 进步?
   - **v0.1.3 能进实现吗?** yes / yes-with-caveats / no。
   - 如果不是 clean yes:**列出「实现前必须修」的项**(严格限定:写代码会真撞墙的),其余归「实现时可定」或「v0.2」。

直接输出。不要 commit、改文件、碰 git。
